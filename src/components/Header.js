import React from 'react';
import './Header.css';  // Opcional: para estilos personalizados

const Header = () => {
    return (
        <header className="header">
            <h1>Bienvenido al BI SaaS</h1>
        </header>
    );
};

export default Header;
